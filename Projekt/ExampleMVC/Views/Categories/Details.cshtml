@model Example.Entities.Models.Category


@using ExampleMVC.Models

@{
    ViewBag.Title = "Details";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Details</h2>

<div>
    <h4>Category</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Name)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Name)
        </dd>

    </dl>
</div>
<p>
    @if (User.Identity.Name == "admin@admin.com")
    {
        @Html.ActionLink("Edit", "Edit", new { id = Model.Id }, new { @class = "btn btn-info btn-sm" })
    }
    <a class="btn btn-primary btn-sm" @Html.ActionLink("Back to List", "Index") </a>
    
    @Html.ActionLink("Products", "CategoryProducts", new { id = Model.Id }, new { @class = "btn btn-warning btn-sm" })
</p>
